<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.mapper.QuestionMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into question (title, type, answer, analysis)
        values (#{title}, #{type}, #{answer}, #{analysis})
    </insert>

    <select id="selectById" resultType="org.example.entity.Question">
        select id, title, type, answer, analysis
        from question
        where id = #{id}
    </select>

    <select id="selectAll" resultType="org.example.entity.Question">
        select id, title, type, answer, analysis
        from question
    </select>

</mapper>
