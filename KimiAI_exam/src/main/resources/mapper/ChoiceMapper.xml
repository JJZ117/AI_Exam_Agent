<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.mapper.ChoiceMapper">

    <!-- 根据题目查选项 -->
    <select id="selectByQuestionId" resultType="org.example.entity.Choice">
        select id, content, question_id as questionId
        from choice
        where question_id = #{questionId}
    </select>

    <!-- 插入 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into choice (content, question_id)
        values (#{content}, #{questionId})
    </insert>

</mapper>